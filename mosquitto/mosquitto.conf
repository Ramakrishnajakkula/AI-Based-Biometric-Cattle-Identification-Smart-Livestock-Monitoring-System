# Mosquitto MQTT Broker Configuration

# Default listener (TCP)
listener 1883
protocol mqtt

# WebSocket listener (for React frontend)
listener 9001
protocol websockets

# Allow anonymous connections (development only)
allow_anonymous true

# Logging
log_type all
log_dest stdout

# Persistence
persistence true
persistence_location /mosquitto/data/
